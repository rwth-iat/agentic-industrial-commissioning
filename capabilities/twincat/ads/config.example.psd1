@{
    # Copy this template to an ignored local file, for example:
    # creds/twincat-ads.local.psd1
    NetId      = '<AMS-NET-ID>'
    SystemPort = 10000
    PlcPort    = 851
    AdsDll     = '<PATH-TO-TWINCAT.ADS.DLL>'
}
